general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore

      
## Deployment options

deployment:
  default:
    branch: [master]
    commands:
      - pip install mkdocs
      - pip install mkdocs-material
      - bower install
      - grunt publish