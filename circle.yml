general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore

      
## Deployment options

deployment:
  default:
    branch: [master]
    commands:
      - pip install mkdocs
      - bower install
      - grunt publish